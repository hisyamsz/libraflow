export const LIMIT_LISTS = [12, 24, 36, 48, 60];
export const DEFAULT_LIMIT = LIMIT_LISTS[0];

export const DEFAULT_PAGE = 1;

export const PAGINATION_DEFAULT = `page=${DEFAULT_PAGE}&limit=${DEFAULT_LIMIT}`;

export const DELAY = 400;
